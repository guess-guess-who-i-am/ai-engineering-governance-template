# 工程上下文与术语

本文件是仓库的 canonical glossary。命名、文档、Issues、Skills 和未来模板代码应使用这些含义。

## 核心概念

**工程治理模板**
: 本仓库提供的可复用起点，包括 Agent 规则、领域上下文、设计上下文、Skills、验证脚本、契约与 Flow 示例。它不是一个完成的业务产品。

**下游项目**
: 从本模板创建的实际产品仓库。下游项目应替换示例、补充自己的领域语义和设计系统，并只保留真正使用的 Skills。

**常驻规则**
: `AGENTS.md` 中几乎每次任务都需要的导航、边界和验证预算。它不能膨胀为所有领域知识的集合。

**领域上下文**
: `CONTEXT.md` 中跨模块稳定的术语、所有权、关系与歧义裁决。它回答“这个概念是什么、归谁负责”。

**设计上下文**
: `DESIGN.md` 中项目当前的视觉 token、组件语言、响应式和交互原则。它不是通用前端教程，也不是第三方品牌官方规范。

**Skill**
: 一套按任务触发的专项工作流，由简洁 `SKILL.md` 和按需 resources 组成。Skill 回答“遇到这一类任务如何工作”。

**确定性门禁**
: 脚本、测试、schema、contract 或 Flow 中可重复执行并给出明确成功/失败的检查。能机械判断的规则应优先成为门禁。

**定性门禁**
: 对审美、意图、清晰度、语气等不能可靠数值化的质量进行盲评。它必须引用具体证据，并在可行时用已知好/坏样例校准；不能替代功能与可访问性检查。

**契约**
: 跨组件消费者可观察的稳定行为，包括输入、输出、失败语义、身份与权限、排序、幂等和兼容策略。

**Flow**
: 可执行的业务或 API 场景，串联真实步骤、变量和断言。Flow 是文档与端到端证据的交集，不替代底层单元测试。

**上游镜像**
: `upstreams/` 中用于研究的第三方 Git 仓库。镜像保留来源历史但不成为本模板的自有代码。

## 关系

- 用户目标决定成功定义。
- `AGENTS.md` 指导 Agent 去哪里找权威、如何控制上下文和验证成本。
- `CONTEXT.md` 约束跨模块语义与所有权。
- `SKILL.md` 只在匹配任务发生时扩展工作流。
- `DESIGN.md` 在 UI 任务中约束视觉与交互语言。
- 契约连接独立组件，Flow 验证跨组件行为。
- 确定性门禁证明可测要求，定性门禁补充不可数值化要求。

## 易混淆项

- **规则 vs Skill**：始终适用的放规则；仅特定任务需要的放 Skill。
- **上下文 vs 文档目录**：上下文只保留稳定语义，不复制全部项目文档。
- **设计系统 vs 风格模仿**：设计系统提供可执行 token 与行为；模仿描述只是灵感。
- **测试 vs Flow**：测试可覆盖局部逻辑；Flow 必须表达真实业务步骤与消费者结果。
- **指标 vs 门禁**：指标是观察值；门禁还必须说明依据、失败含义和处理方式。
- **镜像 vs fork**：镜像用于本地研究；fork 是 GitHub 上可独立维护和贡献的上游分支。

